all:
  children:
    base:
      hosts:
        powereagle:
          ansible_host: localhost
      vars:
        ansible_user: eax
        ansible_connection: local
  vars:
    ansible_python_interpreter: /usr/bin/python3

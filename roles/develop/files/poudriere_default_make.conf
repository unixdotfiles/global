OPTIONS_SET=		ASSERT DEBUG DTRACE \
			OPTIMIZED_CFLAGS ASM SSE SIMD MMX \
			BASH ZSH \
			IPV6 \
			DEVD

lang_gcc_SET=		GRAPHITE
lang_gcc_UNSET=		DEBUG
.for ver in 6 7 8 9
lang_gcc${ver]_SET=	${lang_gcc_SET}
lang_gcc${ver]_UNSET=	${lang_gcc_UNSET}
.endfor
lang_ghc_UNSET=		DEBUG
devel_llvm60_UNSET=	DEBUG
net_wireshark_SET=	ANDROIDDUMP CARES LUA
net_tshark_SET=		${net_wireshark_SET}
math_gmp_SET=		CPU_OPTS
math_R_SET=		RBLAS
math_R_UNSET=		ATLAS
security_sudo_SET=	INSULTS
security_p5-GSSAPI_SET=	GSSAPI_MIT
security_p5-GSSAPI_UNSET=	GSSAPI_BASE

DEFAULT_VERSIONS+=	\
			bdb=6 \
			gcc=7 \
			linux=c7 \
			lua=5.3 \
			mysql=8.0 \
			perl=5.28 \
			pgsql=10 \
			php=7.2 \
			python=3.6 \
			python3=3.6 \
			ruby=2.5 \
			ssl=openssl \
			tcltk=8.7 \
			varnish=5

WITH_DEBUG=	yes
.if ${.CURDIR:M*lang/ruby*} \
	|| ${.CURDIR:M*devel/llvm*} \
	|| ${.CURDIR:M*lang/gcc*} \
	|| ${.CURDIR:M*lang/ghc*} \
	|| ${.CURDIR:M*www/*webkit*} \
	|| ${.CURDIR:M*java/*openjdk*}
.undef WITH_DEBUG
.endif

WITH_PKGNG=devel
CPUTYPE=znver1
